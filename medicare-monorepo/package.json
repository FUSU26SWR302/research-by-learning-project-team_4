{
  "name": "medicare-ai-clinic-monorepo",
  "version": "1.0.0",
  "description": "Medicare AI Clinic - Fullstack Monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names \"BACKEND,FRONTEND\" --prefix-colors \"blue,green\" \"npm run dev --prefix backend\" \"npm run start --prefix frontend\"",
    "start": "concurrently --kill-others-on-fail --names \"BACKEND,FRONTEND\" --prefix-colors \"blue,green\" \"npm run start --prefix backend\" \"npm run start --prefix frontend\"",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run start --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
